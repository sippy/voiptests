diff --git a/bin_interface.c b/bin_interface.c
index 1ca383a23f..68e9d7081c 100644
--- a/bin_interface.c
+++ b/bin_interface.c
@@ -445,7 +445,7 @@ void call_callbacks(char* buffer, struct receive_info *rcv)
 	}
 	if (pkg_len > BIN_MAX_BUF_LEN) {
 		LM_ERR("BIN packet size %u exceeds max size %zu\n",
-				pkg_len, BIN_MAX_BUF_LEN);
+				pkg_len, (size_t)BIN_MAX_BUF_LEN);
 		return;
 	}
 
