#if OPENSIPS_VER_FULL == master || OPENSIPS_VER >= 31
# define NCMDS_BASE 275
#else
#if OPENSIPS_VER >= 24
# define NCMDS_BASE 272
#else
# if OPENSIPS_VER_FULL == 211 /* && OPENSIPS_VER_FULL != 21 && OPENSIPS_VER_FULL != 22 */
#  define NCMDS_BASE 127
# else
#  if OPENSIPS_VER_FULL == 111
#   define NCMDS_BASE 141
#  else
#   if OPENSIPS_VER < 22
#    define NCMDS_BASE 136
#   else
#    if OPENSIPS_VER < 23
#     if OPENSIPS_VER_FULL == 22
#      define NCMDS_BASE 136
#     else
#      define NCMDS_BASE 176
#     endif
#    else
#     if (OPENSIPS_VER_FULL == 23)
#      define NCMDS_BASE 151
#     else
#      define NCMDS_BASE 231
#     endif
#    endif
#   endif
#  endif
# endif
#endif
#endif

#include "rtpproxy.common.output.in"
